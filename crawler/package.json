{
  "name": "crawler",
  "version": "1.0.0",
  "description": "STAC Index crawler",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "docker:build": "docker build -t stac-crawler .",
    "docker:run": "docker run --rm stac-crawler",
    "docker:compose:up": "docker-compose up -d",
    "docker:compose:down": "docker-compose down",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "type": "module",
  "dependencies": {
    "@databases/pg": "^5.5.0",
    "axios": "^1.13.2",
    "dotenv": "^17.2.3",
    "stac-js": "^0.1.9"
  }
}
